## NestJS CLI

```shell
# module, service, controller, dto, entity
nest g resource {name}

nest g module {name}

nest g controller {name}

nest g service {name}

# module, service, controller
nest g module {name} && nest g controller {name} && nest g service {name}
```